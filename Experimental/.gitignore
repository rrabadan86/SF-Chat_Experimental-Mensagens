node_modules/
.env
whatsapp-session/
logs/
# Perfis de navegador (contêm dados sensíveis: cookies, sessões, logins)
chrome-profile/
whatsapp-chrome-data/
evo-chrome-data/
wwebjs_auth/
.wwebjs_auth/
.wwebjs_cache/
instagram-chrome-data/
instagram-login-*.png
# Cookies da sessão do Instagram (contêm o sessionid = acesso à conta) — NUNCA versionar
instagram-cookies.json
# Credenciais (conta de serviço do Google) — NUNCA versionar
service-account.json
*service-account*.json
# Screenshots de debug
debug-*.png
# Edições das mensagens feitas no painel (estado de cada VPS/franquia) — não versionar
data/mensagens.json
# Envios agendados no painel (dados/fotos de cada VPS) — não versionar
data/agendamentos.json
data/agendamentos-fotos/
# Contas do IG que esgotaram tentativas de DM (estado por VPS)
data/instagram-indisponiveis.json
# Estado do WhatsApp (QR/conexao) — por VPS
data/wa-status.json
# Horários dos jobs editados no painel (estado por VPS/franquia) — não versionar
data/horarios.json
# Registro de atividade do robô (o que enviou hoje) — estado por VPS
data/atividade.json
# Ponte de "enviar teste" painel↔robô — estado por VPS
data/teste-envio.json
